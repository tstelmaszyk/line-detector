# Dossiers de build (compilation) — regeneres a tout moment, jamais versionnes.
/build
/build-linux
/build-test
/data

# Sorties du programme.
/out
output.jpg

# Images de test (lourdes / generees) — non versionnees. Le script qui genere
# une image, tools/make_test_image.py, est versionne, lui.
img_piste/

# Fichier parasite macOS.
.DS_Store

# Reglages locaux d'outils dev (permissions personnelles) — non versionnes.
.claude/settings.local.json
